# social-blogging-platform-built-for-the-Data-Association
"A lightweight, document-driven social blogging platform built for the Data Association, leveraging MongoDB’s flexible schema for high-performance content delivery." 

--In this project i almost cover the authentication,authorization and database connectivity part of the backend in node.js.
--used embeded javascript for creating the simple login,logout and post interface (there is no frontend framework) used simple ejs templating
--used jwt as well as cookie parser to verify the previously logged user.
--for databse there is two seprate models for post and the user in user it stores user data and in post save posts(with content) written by the user 


